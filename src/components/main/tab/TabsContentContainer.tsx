import React from "react";

const TabsContentContainer = ({ children }: { children: React.ReactNode }) => {
	return <section className='py-4'>{children}</section>;
};

export default TabsContentContainer;
